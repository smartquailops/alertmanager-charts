apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartquailalm.fullname" . }}
  labels:
    {{- include "smartquailalm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smartquailalm.selectorLabels" . | nindent 4 }}
